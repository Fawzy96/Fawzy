<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mohamedboltia.cargo">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".welcome" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".new_password" />
        <activity android:name=".verify_email" />
        <activity android:name=".add_payment" />
        <activity android:name=".add_new_warehouse" />
        <activity android:name=".current_shipment" />
        <activity android:name=".Enter_Code" />
        <activity android:name=".payment_card" />
        <activity android:name=".Request" />
        <activity android:name=".view_transporter" />
        <activity android:name=".import_export_profile" />
        <activity android:name=".Confirm_R_Activity" />
        <activity android:name=".Show_Activity" />
        <activity
            android:name=".Drawer_Slide_Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps_Activity"
            android:label="@string/title_activity_maps_" />
        <activity
            android:name=".Mapp_Activity"
            android:label="@string/title_activity_mapp_" />
        <activity android:name=".Register_one_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register_Two_Activity" />
        <activity android:name=".Register_Three_Activity"></activity>
    </application>

</manifest>